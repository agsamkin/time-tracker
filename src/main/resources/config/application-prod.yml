spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:}:${DB_PORT:}/time-tracker
    username: ${DB_USERNAME:}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

logging:
  level:
    ROOT: OFF